name: retry_plus
description: Composable retry policies for Dart with exception and result predicates, backoff, jitter, hooks, and cancellation.
version: 0.1.1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  clock: ^1.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
